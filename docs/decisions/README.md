Decision records go here. Name each file `YYYY-MM-DD_<short-slug>.md`.

The record itself should cover: what was decided, what the alternatives were, and
why this option won. Keep it short — a decision record is a permanent note, not a
design doc.
